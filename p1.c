// program to insert substring into string
#include <stdio.h>
void main(){
    int i,j,k,num,last,len_1,len_2,len;
    int position;
    char string[50],insert[50],save[50];
    printf("Enter main string: ");
    scanf("%[^\n]",string);
    printf("%s",string);
    for ( len_1 = 0; string[len_1] != '/0'; len_1++)
    {
        /* code */
        printf("\n length is %d \n",len_1);
        printf("\n position from where it is to be inserted?");
        scanf("%d",&position);
        printf("%d",position);
        printf("Enter the string you have to inserted");
        scanf("%s",insert);
        printf("%s",insert);
        for ( len = 0; i < count; i++)
        {
            /* code */
        }
        


    }
    


}